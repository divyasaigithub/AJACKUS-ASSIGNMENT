# React User Management Application

## Project Setup
1. Clone the repository.
2. Run `npm install` to install dependencies.
3. Run `npm start` to launch the app.

## Directory Structure
- **components/**: Contains reusable components like `App.js`, `UserList.js`, etc.
- **services/**: Includes API service functions.
- **App.css**: Styling for the app.
- **index.js**: Main entry point of the React app.

## Features
- Add/Edit/Delete users.
- Pagination.
- Error handling with `ErrorBoundary`.
- Responsive UI using Bootstrap.

## Challenges
- Ensuring smooth handling of pagination.
- Form validation before submission.

## Improvements
- Implement server-side API for persistent data storage.

